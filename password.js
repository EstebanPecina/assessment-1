///add bolier point code use read line 
//response should come back as an array

// const readline = require('readline');

// const reader = readline.createInterface({
//     input: process.stdin,
//     output: process.stdout,
// });

// reader.question("Welcome to the password validator tool. What password would you like to validate?" , function(input) 
    
// Going over work v//

const readline = require("readLine");

const reader = readline.createInterface({
    input: processingInstruction.stidin,
    output: processingInstruction.stdout
})

reader.question("Welcome to password validator tool.", function(input){ //This reader is going to read what we type in terminal//
console.log(password);

reader.cloer();
}) 